# Legacy live SFTP file. Prefer .env.sftp.live (see .env.sftp.live.example).
# Auto-push never loads this file. Explicit live push still accepts it if
# .env.sftp.live is missing, then requires SFTP_TARGET=dreamhost-live.

SFTP_HOST=iad1-shared-b7-08.dreamhost.com
SFTP_USER=dh_dp755h
SFTP_PASSWORD=replace_me
SFTP_REMOTE_ROOT=bakery.sourflour.org/bake
SFTP_TARGET=dreamhost-live
