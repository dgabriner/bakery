-- =============================================================================
-- Agent 5: ingredient inventory tracking (quantity, reorder level, supplier)
-- Idempotent: ADD COLUMN handled by run_migrations.php when columns are missing
-- Existing ingredients.unit column is reused for stock unit display.
-- =============================================================================

-- No-op placeholder for future backfills; safe to re-run.
