# Checkpoint 0C — Characterization Findings

Generated by `tests/run_characterization.php` against `bakerysf_local`.

## Results

- Passed: 58
- Failed: 0

## Findings (current behavior — do not treat as fixed)

### production_source

production.php aggregates standing_orders for day 1-7, not daily_orders — ignores same-day edits after generation

### delivery_status_split

complete_delivery updates daily_order_assignments.delivery_status only; daily_orders.status is unchanged

## Weekday map (canonical)

| Surface | Sunday encoding |
|---------|-----------------|
| All ops surfaces (standing, generate, pack, bread_distribution, production) | 7 |
| Legacy rows with Sunday stored as 0 | Read via `IN (0,7)`; new writes use 7 |
| Local fixtures | 7 |

## Zone representation

- `customers.zone` stores **text names** matching `zones.name`.
- `customers.zone_id` FK to `zones.id` (migration 004); bread_distribution joins via zone_id with name fallback.

## get_driver_orders.php contract

```
POST application/x-www-form-urlencoded
driver_id={int}&date={YYYY-MM-DD}

Response JSON:
{
  "success": true,
  "orders": [
    {
      "daily_order_id": int,
      "customer_name": string,
      "customer_address": string,
      "zone": string,
      "route_order": int,
      "scheduled_delivery_time": string|null,
      "total_amount": number
    }
  ]
}
```

Canonical SQL source: `driver_list.php` server query joining `daily_orders`, `customers`, `daily_order_assignments`, `drivers` filtered by `doa.driver_id` and `do.order_date`.
