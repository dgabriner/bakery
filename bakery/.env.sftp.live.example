# Copy to .env.sftp.live (gitignored) for explicit production file pushes only.
# Auto-push, Sync, and hooks never load this file.
# Used by: .\scripts\push_sftp.ps1
# Never point this file at staging.sourflour.org or user bakeryOS.

SFTP_HOST=iad1-shared-b7-08.dreamhost.com
SFTP_USER=dh_dp755h
SFTP_PASSWORD=replace_me
SFTP_REMOTE_ROOT=bakery.sourflour.org/bake
SFTP_TARGET=dreamhost-live
